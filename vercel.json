{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "yarn build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.9.1 --activate && yarn install --immutable",
  "regions": ["iad1"]
}
