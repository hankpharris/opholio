{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare yarn@4.9.1 --activate && corepack yarn build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.9.1 --activate && corepack yarn install --immutable",
  "regions": ["iad1"]
}
