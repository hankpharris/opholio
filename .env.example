# Copy this file to `frontend/.env.local` (recommended) or `frontend/.env`.
# Never commit real secrets.
#
# The Next.js app lives in `frontend/` and reads env vars from that directory at runtime.

# ----------------------------
# Public Site URLs (required)
# ----------------------------
# Canonical URL used for metadata/robots/sitemap.
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
#
# Used by some server-side fetch helpers as a fallback when VERCEL_URL is not set.
NEXT_PUBLIC_API_URL="http://localhost:3000"
#
NEXT_PUBLIC_VERCEL_URL="https://your-project.vercel.app"

# ----------------------------
# Database (required)
# ----------------------------
# Prisma/Postgres connection string (works with Neon, Vercel Postgres, etc).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
# Optional unpooled direct connection string (often set by Neon/Vercel integrations).
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
#
# Optional Neon metadata.
NEON_PROJECT_ID=""
#
# Optional Postgres convenience vars auto-injected by Vercel/Neon.
PGDATABASE=""
PGHOST=""
PGHOST_UNPOOLED=""
PGPASSWORD=""
PGUSER=""
POSTGRES_DATABASE=""
POSTGRES_HOST=""
POSTGRES_PASSWORD=""
POSTGRES_PRISMA_URL="postgresql://USER:PASSWORD@HOST:5432/DB?connect_timeout=15&sslmode=require"
POSTGRES_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
POSTGRES_URL_NON_POOLING="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
POSTGRES_URL_NO_SSL="postgresql://USER:PASSWORD@HOST:5432/DB"
POSTGRES_USER=""

# ----------------------------
# Admin Auth (NextAuth + GitHub) (required for /admin)
# ----------------------------
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_URL_INTERNAL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string"
#
# GitHub OAuth app.
# This repo currently expects two credential pairs; for quickstart set *_PERSONAL equal to the primary values.
GITHUB_ID=""
GITHUB_SECRET=""
GITHUB_ID_PERSONAL=""
GITHUB_SECRET_PERSONAL=""
#
# GitHub username allowed to sign in as admin (single-owner).
ALLOWED_GITHUB_USERS="yourgithubusername"

# ----------------------------
# Vercel Blob (required for uploads / background packs)
# ----------------------------
# On Vercel, Blob env vars are typically provisioned automatically when Blob is enabled.
# For local development, you may need to provide a read/write token.
BLOB_READ_WRITE_TOKEN=""
#
# Optional Vercel OIDC token (auto-generated by Vercel CLI in some workflows).
VERCEL_OIDC_TOKEN=""

# ----------------------------
# Optional: Chatbot / TTS (OpenAI)
# ----------------------------
OPENAI_API_KEY=""

# ----------------------------
# Optional: Contact form (Resend)
# ----------------------------
RESEND_API_KEY=""
CONTACT_EMAIL="you@yourdomain.com"

# ----------------------------
# Optional: API CORS allowlist
# ----------------------------
# Comma-separated list. If unset, middleware attempts to infer allowed origins.
CORS_ALLOWED_ORIGINS="http://localhost:3000"
